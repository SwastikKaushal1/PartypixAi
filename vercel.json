{
  "builds": [
    {
      "src": "api/*.py",
      "use": "@vercel/python"
    }
  ],
  "scripts": {
    "build": "apt-get update && apt-get install -y cmake && pip install -r requirements.txt"
  }
}